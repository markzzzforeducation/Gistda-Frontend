# 🍎 คู่มือการตั้งค่าโปรเจคบน Mac

## ปัญหาที่พบบ่อยเมื่อ Clone โปรเจคไป Mac

### ปัญหา: Google OAuth Login ไม่ทำงาน

สาเหตุหลัก:

1. **Environment Variables ไม่ได้ถูกตั้งค่า** - Vite ต้องใช้ไฟล์ `.env` (ไม่ใช่ `config.env`)
2. **Port หรือ Hostname ต่างกัน** - บน Mac อาจจะใช้ port ที่ต่างกัน
3. **Google OAuth Redirect URI ไม่ตรงกัน** - ต้องตั้งค่าให้ตรงกับที่ใช้จริง

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: สร้างไฟล์ `.env`

ใน root directory ของโปรเจค (ที่เดียวกับ `package.json`) สร้างไฟล์ `.env`:

```bash
cp env.example .env
```

หรือสร้างไฟล์ `.env` ใหม่แล้วใส่เนื้อหาดังนี้:

```env
# Frontend Environment Variables
VITE_API_BASE=http://localhost:5174
NEXT_PUBLIC_API_BASE=http://localhost:5174
```

**⚠️ สำคัญ:**

- Vite จะโหลด environment variables จากไฟล์ `.env` เท่านั้น (ไม่ใช่ `config.env`)
- ไฟล์ `.env` จะถูก gitignore ดังนั้นต้องสร้างใหม่ทุกครั้งที่ clone
- ต้อง restart dev server หลังสร้างหรือแก้ไขไฟล์ `.env`

**หมายเหตุ:** โปรเจคนี้ใช้ Vite proxy (ดู `vite.config.ts`) ซึ่งจะ proxy `/api` requests ไปที่ backend โดยอัตโนมัติ ดังนั้น `VITE_API_BASE` อาจจะไม่จำเป็น แต่ควรตั้งค่าไว้เพื่อความแน่ใจ

### ขั้นตอนที่ 2: ตรวจสอบ Port ที่ใช้

1. **เริ่ม Frontend:**
   ```bash
   npm run dev
   ```
2. ดูว่า Vite ใช้ port อะไร (ปกติจะเป็น `5173` แต่ถ้า port นั้นถูกใช้งานแล้ว อาจจะเป็น `5174`, `5175` ฯลฯ)

3. **ถ้า port ไม่ใช่ 5173** ให้แก้ไข:
   - ไฟล์ `.env` - เปลี่ยน `GOOGLE_REDIRECT_URI` ให้ตรงกับ port ที่ใช้
   - Google Cloud Console - เพิ่ม redirect URI ใหม่

### ขั้นตอนที่ 3: ตั้งค่า Google Cloud Console

1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. เลือกโปรเจกต์ของคุณ
3. ไปที่ **APIs & Services** > **Credentials**
4. คลิก OAuth 2.0 Client ID ของคุณ
5. เพิ่ม/แก้ไข **Authorized JavaScript origins:**

   ```
   http://localhost:5173
   http://127.0.0.1:5173
   ```

   (เพิ่มทั้ง localhost และ 127.0.0.1 เพื่อความแน่ใจ)

6. เพิ่ม/แก้ไข **Authorized redirect URIs:**

   ```
   http://localhost:5173/auth/google/callback
   http://127.0.0.1:5173/auth/google/callback
   ```

   (ถ้า port ต่างกัน ให้เพิ่ม port นั้นด้วย)

7. **บันทึกการเปลี่ยนแปลง**

### ขั้นตอนที่ 4: ตรวจสอบ Backend

**สำคัญ:** Backend ต้องตั้งค่า environment variables ให้ถูกต้อง:

ใน backend repository (แยกต่างหาก) สร้างไฟล์ `.env`:

```env
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:5173/auth/google/callback
JWT_SECRET=your_jwt_secret_here
```

**⚠️ ตรวจสอบว่า:**

- `GOOGLE_REDIRECT_URI` ตรงกับ port ที่ frontend ใช้จริง
- Backend กำลังรันอยู่ที่ `http://localhost:5174` (ตามที่ตั้งค่าใน `.env`)

### ขั้นตอนที่ 5: ทดสอบ

1. **เริ่ม Backend:**

   ```bash
   cd backend
   npm install
   npm run dev
   ```

   (ตรวจสอบว่า backend รันที่ port 5174)

2. **เริ่ม Frontend:**

   ```bash
   npm install
   npm run dev
   ```

3. **ทดสอบ Google OAuth:**
   - เปิด browser ไปที่ `http://localhost:5173` (หรือ port ที่ Vite ใช้)
   - เปิด Developer Tools (F12) > Console และ Network tabs
   - คลิก "Continue with Google"
   - ดู error messages ใน Console และ Network

## 🔍 Debug Checklist

ถ้ายังมีปัญหา ให้ตรวจสอบ:

- [ ] มีไฟล์ `.env` ใน root directory แล้ว
- [ ] `VITE_API_BASE` ใน `.env` ตรงกับ port ที่ backend ใช้
- [ ] Google Cloud Console มี redirect URI ตรงกับ port ที่ frontend ใช้
- [ ] Backend กำลังรันอยู่
- [ ] Frontend สามารถเรียก API ได้ (ดูใน Network tab)
- [ ] ไม่มี CORS errors
- [ ] ตรวจสอบ Console logs สำหรับ error messages

## 🐛 ปัญหาที่พบบ่อย

### Error: "redirect_uri_mismatch"

**สาเหตุ:** Redirect URI ใน Google Cloud Console ไม่ตรงกับที่ backend ส่งมา

**แก้ไข:**

1. ตรวจสอบ `GOOGLE_REDIRECT_URI` ใน backend `.env`
2. ตรวจสอบ redirect URIs ใน Google Cloud Console
3. ตรวจสอบว่า URL ตรงกันเป๊ะ (ไม่มี trailing slash, ใช้ http สำหรับ localhost)

### Error: "Network Error" หรือ CORS Error

**สาเหตุ:** Backend ไม่ได้รัน หรือ CORS ไม่ได้ตั้งค่า

**แก้ไข:**

1. ตรวจสอบว่า backend กำลังรันอยู่
2. ตรวจสอบว่า `VITE_API_BASE` ถูกต้อง
3. ตรวจสอบ vite.config.ts proxy settings

### Error: "Cannot read properties of undefined"

**สาเหตุ:** Environment variables ไม่ได้ถูกโหลด

**แก้ไข:**

1. ตรวจสอบว่ามีไฟล์ `.env` (ไม่ใช่ `.env.example`)
2. Restart dev server หลังแก้ไข `.env`
3. ตรวจสอบว่าใช้ prefix `VITE_` สำหรับ environment variables

## 📝 หมายเหตุ

- Vite จะโหลด environment variables จาก `.env`, `.env.local`, `.env.development` ฯลฯ
- ไฟล์ `.env` ควรอยู่ใน `.gitignore` เพื่อไม่ให้ commit secrets
- ใช้ `.env.example` เป็น template สำหรับทีม

## 🆘 ยังแก้ไม่ได้?

1. ตรวจสอบ Console logs ใน browser
2. ตรวจสอบ Network tab ใน Developer Tools
3. ตรวจสอบ backend logs
4. ตรวจสอบว่า backend repository ตั้งค่าถูกต้อง
