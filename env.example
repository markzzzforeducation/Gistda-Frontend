# Frontend Environment Variables
# Vite จะโหลด environment variables ที่มี prefix VITE_ เท่านั้น
# แต่เรารองรับทั้งสองแบบเพื่อความเข้ากันได้
VITE_API_BASE=http://localhost:5174
NEXT_PUBLIC_API_BASE=http://localhost:5174

# Backend Environment Variables
# หมายเหตุ: ตัวแปรเหล่านี้ใช้ใน backend repository แยกต่างหาก
# ไม่ได้ใช้ใน frontend แต่วางไว้เพื่อ reference
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:5173/auth/google/callback
JWT_SECRET=your_jwt_secret_here
